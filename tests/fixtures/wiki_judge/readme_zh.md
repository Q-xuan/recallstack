# DeepSeek Harness

DeepSeek Harness（`dsh`）是由 DeepSeek AI 开发的开源 agent harness（智能体框架）。

它采用**一切皆插件**的架构，并由 Cordis 驱动。

## 运行

安装 `Node.js`，然后运行：

```sh
npx @deepseek-ai/dsh web
```

该命令会启动 Web UI，默认地址为 `http://127.0.0.1:3080`。

## 开发

请先阅读开发指南与架构文档。面向 agent：请遵循 AGENTS.md。
